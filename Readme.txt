To run application please install node.js and mongodb

-After node.js and mongodb installation we need to create a new database named test

-After that we need to install the MongoDB Node.js Driver.

-Then to execute the application node server.js

-Then to see on browser go to http://localhost:133

